# Debian Repo

We have a custom debian repo that primarily the computer science team uses for installing software. The main goal is that a new member can simply run:
  aptitude install robosub
and have all the necessary software installed on their computer.